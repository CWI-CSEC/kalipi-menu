#!/bin/bash

read -sn1 a
test "$a" == `echo -en "\e"` || continue
read -sn1 a
test "$a" == "[" || continue
read -sn1 a
case "$a" in
  A)  keyinput=0;; #UP
  B)  keyinput=1;; #DOWN
  C)  keyinput=2;; #RIGHT
  *)  exit
esac
echo $keyinput
